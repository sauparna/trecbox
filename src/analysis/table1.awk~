# write out a table for all test collections for one measure only.

BEGIN{
     while(getline l <"../../viz/tab0.rand")
     {
	  # for (i=2; i<=NF; i+=5)
	  #      a[$1] = a[$1] " " $i

	  for (i=2; i<=NF; i+=5)
	       a = a " " $i

	  print a
     }
}